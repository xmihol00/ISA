Tato část dokumentu popisuje Trivial File Transfer Protocol (TFTP) i s jeho zpětně kompatibilním rozšířením
o možnou specifikaci podmínek přenosu, jehož neoficiální zkratkou je TFTPv2. Rozšiřující podmínky zahrnují možnost
klienta specifikovat velikost přenášeného bloku dat, specifikovat --timeout-- pro znovu odeslání datagramu
a možnost získat velikost přenášeného souboru před začátkem přenosu jeho dat.

TFTP je protokol aplikační vrstvy a jako transportní vrstvu využívá User Datagram Protocol (UDP). Protokol je bezstrátový,
bezstrátovost zajišťuje na aplikační vsrtvě podobně jako TCP na vrstvě transportní, a to pomocí --ACK-- datagramů. 
Nedochází ale k vytvoření spojení.

TODO - UPD a TCP zmínka.

V následujícím diagramu je vyjádřena komunikace mezi serverem a klientem.

sudo service tftpd-hpa status
sudo service tftpd-hpa stop

==42110== Memcheck, a memory error detector
==42110== Copyright (C) 2002-2017, and GNU GPL'd, by Julian Seward et al.
==42110== Using Valgrind-3.15.0 and LibVEX; rerun with -h for copyright info
==42110== Command: ./mytftpclient
==42110== 
> -W -d file1 -a 192.168.1.147,69

Writing file 'file1' succeeded.
Transfer summary:
   - 31 ms elapsed during the transfer,
   - 737 B of data were sent in 2 datagrams of a maximum data size 512 B.

> -W -d file2 -a 2001:db8:1:1:204:76ff:fe47:c,69

Writing file 'file2' succeeded.
Transfer summary:
   - 1 ms elapsed during the transfer,
   - 873 B of data were sent in 2 datagrams of a maximum data size 512 B.

> -R -d file2 -a 192.168.1.147,69
Reading file 'file2' ...

Reading file 'file2' succeeded.
Transfer summary:
   - 11 ms elapsed during the transfer,
   - 873 B of data were recieved in 2 datagrams of a maximum data size 512 B.

> -R -d file1 -a 2001:db8:1:1:204:76ff:fe47:c,69
Reading file 'file1' ...

Reading file 'file1' succeeded.
Transfer summary:
   - 2 ms elapsed during the transfer,
   - 737 B of data were recieved in 2 datagrams of a maximum data size 512 B.

> q
==42110== 
==42110== HEAP SUMMARY:
==42110==     in use at exit: 0 bytes in 0 blocks
==42110==   total heap usage: 27 allocs, 27 frees, 95,530 bytes allocated
==42110== 
==42110== All heap blocks were freed -- no leaks are possible
==42110== 
==42110== For lists of detected and suppressed errors, rerun with: -s
==42110== ERROR SUMMARY: 0 errors from 0 contexts (suppressed: 0 from 0)
